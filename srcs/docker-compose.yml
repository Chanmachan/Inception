version: '3'

services:
  nginx:
    container_name: nginx
    build: ./requirements/nginx
    ports:
      - "443:443"
    volumes:
      - wp_web_files:/var/www/html
    depends_on:
      - wordpress
    restart: always
    networks:
      - inception_network